multiplicador = int(input("Introduzca el número por el cual quiere que sea multiplicado por 3: "))

resultado = multiplicador * 3

def multiplicacion(multiplicador, resultado):

       multiplicador * 3
       print (resultado)

print(multiplicacion(multiplicador, resultado ))
